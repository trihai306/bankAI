# Core ML
# torch 2.5.1 is the last version where torchaudio supports soundfile backend natively.
# torchaudio 2.9+ requires torchcodec which has DLL issues on Windows.
torch==2.5.1
torchaudio==2.5.1

# F5-TTS dependencies (installed separately via pip install -e)
# These are common deps that F5-TTS needs
soundfile>=0.13.0
librosa>=0.10.2
numpy>=1.24.0,<=1.26.4  # F5-TTS requires numpy<=1.26.4

# Utilities
jieba>=0.42.1
pypinyin>=0.53.0
