# Core ML
# NOTE: torch<2.6.0 required for CUDA sm_61 (GTX 1060) support
torch>=2.4.0,<2.6.0
torchaudio>=2.4.0,<2.6.0

# F5-TTS dependencies (installed separately via pip install -e)
# These are common deps that F5-TTS needs
soundfile>=0.13.0
librosa>=0.10.2
numpy>=1.24.0,<=1.26.4  # F5-TTS requires numpy<=1.26.4

# Utilities
jieba>=0.42.1
pypinyin>=0.53.0
